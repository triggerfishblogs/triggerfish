/**
 * Triggerfish runtime — root exports for version and package metadata.
 *
 * @module
 */

/** Semantic version of the Triggerfish runtime. */
export const VERSION = "0.1.0";

/** Package name. */
export const NAME = "triggerfish";
