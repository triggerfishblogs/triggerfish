# Reference

இந்த section Triggerfish configure செய்து extend செய்வதற்கான technical reference material வழங்குகிறது.

## [Config Schema](./config-yaml)

`triggerfish.yaml` க்கான Full reference -- ஒவ்வொரு section, ஒவ்வொரு key, annotated example உடன். Models, channels, classification, policy, MCP servers, scheduler, மற்றும் notifications cover செய்கிறது.

## [Key Interfaces](./interfaces)

Integrations, custom channel adapters, LLM providers, அல்லது storage backends build செய்யும் developers க்கான TypeScript interface reference. `StorageProvider`, `ChannelAdapter`, `LlmProvider`, `NotificationService`, hook types, session state, மற்றும் `Result<T, E>` pattern சேர்க்கிறது.

## [Glossary](./glossary)

Buoy இலிருந்து Write-Down வரை ஒவ்வொரு Triggerfish-specific term க்கான Definitions.
