# Reference

یہ section Triggerfish configure اور extend کرنے کے لیے technical reference
material فراہم کرتا ہے۔

## [Config Schema](./config-yaml)

`triggerfish.yaml` کا مکمل reference — ہر section، ہر key، annotated example کے
ساتھ۔ Models، channels، classification، policy، MCP servers، scheduler، اور
notifications cover کرتا ہے۔

## [Key Interfaces](./interfaces)

Integrations، custom channel adapters، LLM providers، یا storage backends build
کرنے والے developers کے لیے TypeScript interface reference۔ `StorageProvider`،
`ChannelAdapter`، `LlmProvider`، `NotificationService`، hook types، session state،
اور `Result<T, E>` pattern شامل ہیں۔

## [Glossary](./glossary)

Triggerfish-specific ہر term کی definitions، Buoy سے Write-Down تک۔
