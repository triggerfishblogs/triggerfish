# Reference

हे section Triggerfish configure आणि extend करण्यासाठी technical reference material
provide करतो.

## [Config Schema](./config-yaml)

`triggerfish.yaml` साठी Full reference -- प्रत्येक section, प्रत्येक key, annotated
example सह. Models, channels, classification, policy, MCP servers, scheduler,
आणि notifications cover करतो.

## [Key Interfaces](./interfaces)

Integrations, custom channel adapters, LLM providers, किंवा storage backends
build करणाऱ्या developers साठी TypeScript interface reference. `StorageProvider`,
`ChannelAdapter`, `LlmProvider`, `NotificationService`, hook types, session state,
आणि `Result<T, E>` pattern include करतो.

## [Glossary](./glossary)

Buoy ते Write-Down पर्यंत प्रत्येक Triggerfish-specific term साठी Definitions.
